let age: number;
let username: string;
let toggle: true;
let empty: null;
let callback = (a: number): number => {
  return 100 + a;
};

age = 50;
username = "Max";
toggle = true;
empty = null;
callback(5);
